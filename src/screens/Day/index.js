import React from "react";
import { Text, View } from "react-native";

import styles from "./styles";

const DayScreen = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Pill ssscreen</Text>
    </View>
  );
};

export default DayScreen;
